<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    # Canonical production HTTPS before Laravel routing.
    RewriteCond %{HTTP_HOST} ^weofferwellness\.co\.uk$ [NC]
    RewriteRule ^ https://www.weofferwellness.co.uk%{REQUEST_URI} [R=301,L,NE]

    # Behind Cloudflare, trust an explicit visitor scheme first. This avoids a
    # redirect loop if the edge is HTTPS but the origin connection is HTTP.
    RewriteCond %{HTTP_HOST} ^www\.weofferwellness\.co\.uk$ [NC]
    RewriteCond %{HTTP:X-Forwarded-Proto} ^http$ [NC]
    RewriteRule ^ https://www.weofferwellness.co.uk%{REQUEST_URI} [R=301,L,NE]

    # Direct-origin fallback when there is no forwarded scheme header.
    RewriteCond %{HTTP_HOST} ^www\.weofferwellness\.co\.uk$ [NC]
    RewriteCond %{HTTP:X-Forwarded-Proto} ^$
    RewriteCond %{HTTPS} !=on
    RewriteRule ^ https://www.weofferwellness.co.uk%{REQUEST_URI} [R=301,L,NE]

    # Handle Authorization Header
    RewriteCond %{HTTP:Authorization} .
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Handle X-XSRF-Token Header
    RewriteCond %{HTTP:x-xsrf-token} .
    RewriteRule .* - [E=HTTP_X_XSRF_TOKEN:%{HTTP:X-XSRF-Token}]

    # Redirect Trailing Slashes If Not A Folder...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_URI} (.+)/$
    RewriteRule ^ %1 [L,R=301]

    # Send Requests To Front Controller...
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [L]
</IfModule>
